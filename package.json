{
  "name": "weflair-site",
  "private": true,
  "version": "1.0.0",
  "description": "WeFlair Growth Marketing Agency — Static site with custom foundation CSS",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5995",
    "build": "node build-site.cjs && node generate-seo.cjs",
    "preview": "npx serve dist -p 3000"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "jsdom": "^29.1.1",
    "vite": "^6.4.2"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "playwright-core": "^1.59.1"
  }
}
